pipeline{
    agent any
    stages{
        stage ('Checkout'){
            steps{
                echo "Getting code from GitHub"
            }
        }

        // stage ('Build Docker Image'){
        //     steps{
        //         sh 'docker build -t docker-demo .'
        //     }
        // }

        // stage('Run Container') {
        //     steps {
        //         sh 'docker run --rm  docker-demo'
        //     }
        // }

        stage ('Docker Build'){
            steps{
                sh 'docker build -t oforiwaa/devops-demo:latest .'
            }
        }

        stage ('Docker Push'){
            steps{
                withCredentials([usernamePassword(credentialsId: 'dockerhub-credentials', passwordVariable: 'DOCKER_PASSWORD', usernameVariable: 'DOCKER_USERNAME')]) {
                    sh '''
                    echo $DOCKER_PASSWORD | docker login -u $DOCKER_USERNAME --password-stdin
                    docker push  "oforiwaa/devops-demo:latest"
                    docker logout
                    '''
                }
            }
        }

            stage ('Docker Run'){
                steps{
                    sh 'docker run --rm oforiwaa/devops-demo:latest'
                }
            }

        stage ('Test'){
            steps{
                echo "Running tests"
            }
        }

        stage ("Deploy"){
            steps{
                echo "Deploying application"
            }
        }
    }
}